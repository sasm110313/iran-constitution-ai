# ⚖️ سامانه هوشمند تحلیل و تطبیق حقوقی قانون اساسی جمهوری اسلامی ایران
### Iran Constitution AI Legal Analysis & Search Engine

سامانه هوشمند تحلیل و تطبیق حقوقی قانون اساسی، ابزاری پیشرفته و پرسرعت برای پردازش، استعلام حقوقی موضوعی و بررسی مطابقت یا مغایرت مصوبات مجلس شورای اسلامی با اصول ۱۷۷ گانه قانون اساسی جمهوری اسلامی ایران است.

---

## 🌟 ویژگی‌های کلیدی سامانه

1. **ارزیابی پرسرعت و موازی (Parallel Evaluation):**
   - اسکن و پردازش هم‌زمان تمامی **۱۷۷ اصل قانون اساسی** در زیر ۲ ثانیه.
   - محاسبه دقیق درصد مطابقت حقوقی و درجه صراحت احکام بدون ایجاد توهم یا متن خروجی ساختگی.

2. **تنظیم و مدیریت امن کلید API (Dynamic API Key Management):**
   - هیچ کلید API در سورس کد یا گیت‌هاب قرار ندارد.
   - کاربر می‌تواند کلید API خود را به صورت مستقیم در **تنظیمات وب‌سایت** (ذخیره‌سازی محلی در مرورگر) یا از طریق متغیر محیطی `TYPESAFE_API_KEY` وارد نماید.

3. **ماژول تخصصی بررسی مصوبات مجلس (ویژه شورای نگهبان):**
   - تنظیم آستانه ۴۰٪ برای بررسی کامل و هوشمند مواد و بندهای مصوب مجلس و استخراج اصول مرتبط یا مغایرت‌های احتمالی.

4. **دسته‌بندی و فیلترینگ بر اساس فصل‌های قانون اساسی:**
   - قابلیت فیلتر بر اساس فصل‌های ۱۴ گانه (حقوق ملت، قوه مقننه، قوه قضائیه، رهبری، اقتصاد و امور مالی و...).

5. **تولید و کپی گزارش رسمی حقوقی (Export & Print):**
   - قابلیت تولید خلاصه گزارش متنی جهت استفاده در مکاتبات رسمی حقوقی و نسخه چاپی بهینه.

---

## 🛠️ ساختار پروژه

```text
├── app.py                   # سرور اصلی Flask و تعریف REST API
├── jev_legal_engine.py      # موتور پردازش حقوقی و ارزیابی موازی اصول
├── parse_constitution.py    # پارسر متن قانون اساسی به فرمت ساختاریافته JSON
├── data/
│   └── constitution_fa.json # پایگاه کامل ۱۷۷ اصل قانون اساسی به همراه فصل‌ها
├── templates/
│   └── index.html           # رابط کاربری وب (داشبورد هوشمند با امکان وارد کردن کلید API)
└── venv/                    # محیط مجازی پایتون
```

---

## 🚀 راهنمای نصب و اجرا

### ۱. پیش‌نیازها
- Python >= 3.10

### ۲. ایجاد محیط مجازی و نصب وابستگی‌ها

```bash
# ایجاد محیط مجازی
python3 -m venv venv

# فعال‌سازی محیط مجازی (Linux/macOS)
source venv/bin/activate

# نصب پکیج‌های مورد نیاز
pip install flask httpx
```

### ۳. تنظیم کلید API

می‌توانید کلید API را به یکی از دو روش زیر تنظیم کنید:

- **روش اول (از طریق محیط وب):** پس از اجرای برنامه، روی دکمه «🔑 تنظیم کلید API» در بالای صفحه کلیک کرده و کلید خود را وارد کنید.
- **روش دوم (متغیر محیطی):**
  ```bash
  export TYPESAFE_API_KEY="your_api_key_here"
  ```

### ۴. اجرای برنامه

```bash
python3 app.py
```

پس از اجرای برنامه، مرورگر خود را باز کرده و به آدرس زیر بروید:
👉 **`http://127.0.0.1:5000`**

---

## 📄 مجوز و انتشار (License)
این پروژه تحت مجوز MIT منتشر شده است و برای تمامی پژوهشگران حقوقی، سازمان‌ها و مراجع قانونی قابل توسعه و استفاده می‌باشد.
